## Agent skills

### Issue tracker

Spec Kit specifications and tasks are the local issue tracker. See `docs/agents/issue-tracker.md`.

### Triage labels

Local `Status:` fields use the standard triage roles. See `docs/agents/triage-labels.md`.

### Domain docs

This repository uses the single-context domain layout. See `docs/agents/domain.md`.
